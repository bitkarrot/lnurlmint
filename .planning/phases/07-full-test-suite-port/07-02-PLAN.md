---
phase: 07-full-test-suite-port
plan: 02
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - tests/conftest.py
  - tests/test_lnurlcash.py
  - tests/test_verify.py
  - tests/test_comment_protection.py
  - tests/test_surface_hunter.py
  - tests/test_auth_data_hunter.py
  - tests/test_mark_pending.py
  - tests/test_reconcile.py
autonomous: true
requirements:
  - TEST-10
must_haves:
  truths:
    - All remaining test files from the source are ported and pass against LNbits fixtures.
    - test_lnurlcash.py (55 source tests) is ported as the main behavioral regression suite.
    - test_verify.py (15 ported tests, 2 migration tests N/A) is ported for LUD-21 verify coverage.
    - test_comment_protection.py (11 tests) is ported for LUD-25 comment protection coverage.
    - test_surface_hunter_verification.py (4 ported tests, 1 N/A) is ported for security review coverage.
    - test_auth_data_hunter_poc.py (3 tests) is ported for security review coverage.
    - test_poc_a3_mark_pending.py (3 tests) is ported as test_mark_pending.py.
    - test_reconcile.py (4 ported tests, 3 adapted) is ported for reconcile coverage.
    - Already-ported tests (Phases 2-6) are NOT re-ported — listed as skip.
    - N/A tests (7 files, 69 tests) are documented with justification — not ported.
    - Full test suite runs as a regression gate with no failures.
  artifacts:
    - path: tests/test_lnurlcash.py
      provides: Main behavioral test suite (mint/melt/rotate/split/merge/fees/sunset/pending)
      contains: "test_pay_request_advertises"
    - path: tests/test_verify.py
      provides: LUD-21 verify endpoint tests (mint + melt directions)
      contains: "test_verify_url_absent"
    - path: tests/test_comment_protection.py
      provides: LUD-25 comment protection tests
      contains: "test_valid_comment_credits"
    - path: tests/test_surface_hunter.py
      provides: Security review surface lane tests (P3 collision, P1 verify gating)
      contains: "test_p3_rotate"
    - path: tests/test_auth_data_hunter.py
      provides: Security review auth-data lane tests (F1 verify, F3 pending, F4 collision)
      contains: "test_f1_verify"
    - path: tests/test_mark_pending.py
      provides: mark_pending validation tests (every id validated, no partial reservation)
      contains: "test_a3_mark_pending"
    - path: tests/test_reconcile.py
      provides: Reconcile tests (finalize, restore, leave-pending, error logging)
      contains: "test_reconcile_finalizes"
    - path: tests/conftest.py
      provides: Additional helpers (note_value, _leave_a_note_pending, FakeNode fee tracking)
      contains: "note_value"
  key_links:
    - from: tests/test_lnurlcash.py
      to: lnurlmint/views_lnurl.py
      via: "get_withdraw, get_withdraw_callback, get_pay_callback, get_payrequest direct calls"
      pattern: "get_withdraw|get_pay"
    - from: tests/test_verify.py
      to: lnurlmint/views_lnurl.py
      via: "get_verify, get_pay_callback, get_withdraw_callback direct calls"
      pattern: "get_verify"
    - from: tests/test_comment_protection.py
      to: lnurlmint/crud.py
      via: "get_note, get_pending_mint_record for checking note state"
      pattern: "get_note"
    - from: tests/test_reconcile.py
      to: lnurlmint/services.py
      via: "reconcile_pending_melts direct call"
      pattern: "reconcile_pending_melts"
---

## Phase Goal

**As a** developer, **I want to** run the complete ported test suite as a regression gate, **so that** behavioral parity with `lnurl-mint` is verified and no funds-loss regression exists across all features.

## Acceptance Criteria

- [ ] `tests/test_lnurlcash.py` is created with ported tests from source `test_lnurlcash.py` (55 tests adapted to LNbits fixtures).
- [ ] `tests/test_verify.py` is created with ported tests from source `test_verify.py` (15 tests; 2 migration tests N/A).
- [ ] `tests/test_comment_protection.py` is created with 11 ported tests from source `test_comment_protection.py`.
- [ ] `tests/test_surface_hunter.py` is created with 4 ported tests from source `test_surface_hunter_verification.py` (1 N/A).
- [ ] `tests/test_auth_data_hunter.py` is created with 3 ported tests from source `test_auth_data_hunter_poc.py`.
- [ ] `tests/test_mark_pending.py` is created with 3 ported tests from source `test_poc_a3_mark_pending.py`.
- [ ] `tests/test_reconcile.py` is created with 4 ported tests from source `test_reconcile.py` (3 server-lifespan tests adapted to function calls).
- [ ] Already-ported tests (test_offline_verification.py, test_onion.py, test_poc_verify_race.py, etc.) are NOT re-ported.
- [ ] N/A tests (test_config.py, test_errors.py, test_frontend.py, test_mint_log.py, test_node.py, test_server.py, test_poc_rpc_census.py) are documented as N/A with justification.
- [ ] Full test suite passes with no regressions: `python -m pytest tests/ -v` all green.
- [ ] `tests/conftest.py` has any additional helpers needed (note_value, _leave_a_note_pending, FakeNode fee tracking).

## Artifacts This Phase Produces

This plan produces the complete ported test suite — the behavioral parity acceptance gate. All remaining test files from the source are ported, adapted to LNbits fixtures and wallet mocks. Already-ported tests (Phases 2-6) and N/A tests (LNbits extension model) are documented but not re-ported. The full suite runs as a regression gate, verifying no funds-loss regression across all features: mint, melt, rotate, split, merge, fees, sunset, comment protection, verify, offline verification, Tor, reconcile, and all security PoCs.

<threat_model>
ASVS Level: 1 | Blocking Threshold: high

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Behavioral parity regression (mint/melt/rotate/split/merge) | high | Full test_lnurlcash.py suite (55 tests) covers all lifecycle operations with edge cases |
| Verify endpoint regression (preimage leak, off-switch) | high | test_verify.py (15 tests) covers advertisement, settlement status, preimage gating, melt verify, off-switch |
| Comment protection regression (preimage race) | high | test_comment_protection.py (11 tests) covers valid/invalid/absent comment, collisions, redeem operations |
| Reconcile regression (stranded notes, double-spend) | high | test_reconcile.py (4 tests) covers finalize, restore, leave-pending, error logging |
</threat_model>

<objective>
Port all remaining test files from the source, adapted to LNbits test fixtures and wallet mocks, and run the complete suite as a regression gate.

Purpose: This is the behavioral parity acceptance gate — every test from the source that applies to the LNbits extension model is ported and passing, completing the full LUD-25 behavioral parity verification.
Output: 7 new test files with ~95 ported test functions, full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-full-test-suite-port/07-CONTEXT.md
@.planning/phases/07-full-test-suite-port/07-RESEARCH.md
@.planning/phases/07-full-test-suite-port/07-01-PLAN.md
@tests/conftest.py
@tests/test_poc_verify_race.py
@tests/test_offline_verification.py
@tests/test_onion.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add conftest.py helpers for remaining tests</name>
  <files>
    tests/conftest.py
  </files>
  <read_first>
    <file>tests/conftest.py</file>
    <file>~/lnurl-mint/tests/test_lnurlcash.py</file>
    <file>~/lnurl-mint/tests/test_reconcile.py</file>
  </read_first>
  <behavior>
    - tests/conftest.py contains an async `note_value(k1: str) -> int | None` helper (if not already added in Plan 07-01)
    - tests/conftest.py contains an async `_leave_a_note_pending(node, amount_msat=5000) -> str` helper that mints a note, then melts it with an unconfirmable payment (HodlNode fail_payments + is_payment_complete_raises), leaving it pending, and returns k1
    - FakeNode class has a `last_fee_limit_msat` attribute that tracks the fee_limit_msat passed to pay_invoice (for melt fee limit tests)
    - FakeNode class has a `check_transaction_status_calls` counter (for tests asserting bounded confirmation attempts)
  </behavior>
  <action>
    1. If `note_value` is not already in conftest.py (from Plan 07-01), add it: async function that calls `get_withdraw(TEST_MINT_ID, _mock_request(), k1=k1)` and returns `maxWithdrawable` or `None`.

    2. Add `_leave_a_note_pending(node, amount_msat=5000)` async helper:
       - `k1, note_id, mint = await mint_note(node, amount_msat)`
       - Set `node.fail_payments = True` and `node.is_payment_complete_raises = True`
       - `pr = fake_invoice(amount_msat)`
       - `await get_withdraw_callback(TEST_MINT_ID, _mock_request(), BackgroundTasks(), k1=[k1], pr=pr)`
       - Verify pending: `await get_withdraw(TEST_MINT_ID, _mock_request(), k1=k1)` → "pending"
       - Return k1

    3. Add `last_fee_limit_msat` to FakeNode.__init__: `self.last_fee_limit_msat = None`. In `pay_invoice`, capture the fee_limit_msat parameter. Note: the port's FakeNode.pay_invoice signature is `async def pay_invoice(self, *, wallet_id, payment_request, **kwargs)` — the fee limit is passed as a kwarg or via a separate mechanism. Check how `services.py` calls `lnbits_pay_invoice` and adapt. If the fee limit is not directly passed to the fake, add a monkeypatch or wrapper to capture it.

    4. Add `check_transaction_status_calls` counter to FakeNode.__init__: `self.check_transaction_status_calls = 0`. Increment in `check_transaction_status`.
  </action>
  <verify>
    <automated>python -c "from lnurlmint.tests.conftest import note_value, _leave_a_note_pending; print('ok')"</automated>
  </verify>
  <done>All helpers are defined and importable. FakeNode has last_fee_limit_msat and check_transaction_status_calls.</done>
</task>

<task type="auto">
  <name>Task 2: Port test_lnurlcash.py — main behavioral suite</name>
  <files>
    tests/test_lnurlcash.py
  </files>
  <read_first>
    <file>~/lnurl-mint/tests/test_lnurlcash.py</file>
    <file>tests/conftest.py</file>
    <file>tests/test_poc_verify_race.py</file>
    <file>.planning/phases/07-full-test-suite-port/07-RESEARCH.md</file>
  </read_first>
  <behavior>
    - tests/test_lnurlcash.py contains ported tests from source test_lnurlcash.py, adapted to LNbits fixtures
    - All tests use @pytest.mark.anyio and direct endpoint calls (no TestClient)
    - Tests cover: payRequest advertisement, pay callback (minting), fee math, sunset mode, withdraw informational endpoint, rotate, split, merge, melt, pending states, k1 validation, Host-header spoofing, no-secret-persisted
    - The note_value helper from conftest.py is used to check note values
    - update_mint is used for config changes (fees, sunset, verify_enabled) instead of monkeypatching settings
    - inflight_node fixture is used for pending/in-flight tests instead of threading
    - Tests that overlap with existing PoCs are still ported for broader edge-case coverage
  </behavior>
  <action>
    Create `tests/test_lnurlcash.py` with ported versions of all 55 source tests. Group them logically:

    **PayRequest + pay callback (13 tests):**
    - test_pay_request_advertises_withdraw_link → `await get_payrequest(TEST_MINT_ID, _mock_request())`, check tag, withdrawLink, minSendable <= maxSendable
    - test_paid_invoice_preimage_becomes_a_bearer_note → mint via get_pay_callback, settle, check note_value
    - test_pay_callback_advertises_not_disposable → check disposable: false in get_pay_callback response
    - test_pay_callback_enforces_sendable_bounds → amount=1 and amount=999999999999 → ERROR
    - test_pay_callback_rejects_while_sunsetting → update_mint sunset_mint=True → ERROR
    - test_pay_response_omits_mint_fee_when_free → check metadata has no "Mint fees:"
    - test_pay_response_advertises_mint_fee_when_configured → update_mint base_fee_msat=1000, fee_percent_ppm=2000 → check metadata
    - test_pay_response_advertises_fee_inclusive_min_sendable → update_mint fees → check minSendable is fee-aware
    - test_mint_credits_note_net_of_configured_fee → check note value = amount - fee
    - test_mint_fee_rounds_up_to_the_nearest_sat → check rounding up
    - test_pay_callback_rejects_amount_that_cannot_cover_the_fee → ERROR
    - test_pay_callback_rejects_amount_below_min_mint → ERROR
    - test_mint_succeeds_at_exactly_min_mint → boundary test

    **Withdraw callback URL + verify URL (2 tests):**
    - test_withdraw_callback_url_ignores_spoofed_host_header → mock request with evil host, check callback URL uses base_url
    - test_verify_url_ignores_spoofed_host_header → similar for verify URL

    **Rotate (1 test):**
    - test_rotate_burns_and_replaces_the_note → rotate, check old burned, new has value

    **Split (8 tests):**
    - test_split_mints_amount_and_change, test_split_merges_multiple_k1s_first, test_split_rejects_amount_out_of_range, test_split_rejects_while_sunsetting, test_split_deducts_base_fee_from_change, test_split_does_not_reapply_fee_percent_ppm, test_split_rejects_when_change_cannot_cover_base_fee, test_split_rejects_zero_value_change, test_split_ignores_min_mint_msat

    **Merge (3 tests):**
    - test_merge_burns_all_and_mints_the_sum, test_merge_refunds_base_fee_for_every_extra_note, test_rotate_is_unaffected_by_mint_fees

    **Melt (16 tests):**
    - test_melt_pays_invoice_of_exactly_the_notes_value, test_melt_fee_limit_defaults_to_baseline, test_melt_fee_limit_follows_higher_mint_fee, test_melt_rejects_multiple_k1s, test_melt_rejects_invoice_of_wrong_amount, test_failed_payment_restores_the_notes, test_pending_note_rejects_concurrent_operations (use inflight_node), test_pending_note_is_released_if_payment_fails, test_payment_failed_still_confirms_before_restoring, test_pending_note_is_released_if_funding_source_unavailable, test_melt_rejects_own_pending_invoice, test_melt_rejects_already_settled_own_invoice, test_ambiguously_failed_payment_that_succeeded_does_not_restore, test_undeterminable_payment_status_leaves_pending, test_hodl_invoice_attack_leaves_pending, test_undeterminable_payment_status_retries_before_giving_up, test_rotate_merge_and_melt_unaffected_by_sunsetting

    **Validation + edge cases (12 tests):**
    - test_any_invalid_k1_fails_whole_request, test_duplicate_k1_cannot_be_double_counted, test_too_many_k1s_is_rejected, test_amount_cannot_be_combined_with_pr, test_withdraw_response_echoes_literal_secret, test_withdraw_requires_k1, test_withdraw_reports_unknown_k1_distinctly_from_spent, test_withdraw_ignores_declared_amount, test_no_bearer_secret_is_ever_persisted, test_spent_k1_cannot_be_replayed

    For each test:
    - Replace `client.get("/p/cb?amount=X")` → `await get_pay_callback(TEST_MINT_ID, _mock_request(), amount=X)`
    - Replace `client.get("/w?k1=X")` → `await get_withdraw(TEST_MINT_ID, _mock_request(), k1=X)`
    - Replace `client.get("/w/cb?k1=X&h=Y")` → `await get_withdraw_callback(TEST_MINT_ID, _mock_request(), BackgroundTasks(), k1=[X], h=Y)`
    - Replace `notes.note_amount(h)` → `await get_note(h)` and check `.amount_msat`
    - Replace `monkeypatch.setattr(settings, "base_fee_msat", X)` → `await update_mint(TEST_MINT_ID, TEST_WALLET, base_fee_msat=X)`
    - Replace `monkeypatch.setattr(settings, "sunset_mint", True)` → `await update_mint(TEST_MINT_ID, TEST_WALLET, sunset_mint=True)`
    - Replace `node.last_preimage.hex()` → `node.preimages[payment_hash]` (get payment_hash from the pay callback response or the create_invoice return)
    - Replace `node.last_fee_limit_msat` → `node.last_fee_limit_msat` (added in Task 1)
    - Replace `node.is_payment_complete_called` → `node.check_transaction_status_calls > 0`
    - Replace `melt_in_background` → use `inflight_node` fixture with `pay_release` event control
  </action>
  <verify>
    <automated>cd /home/exedev/lnurlmint && python -m pytest tests/test_lnurlcash.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>All ported tests in test_lnurlcash.py pass.</done>
  <acceptance_criteria>
    - `python -m pytest tests/test_lnurlcash.py -v` shows all tests passed
    - No test uses TestClient
    - All async tests use @pytest.mark.anyio
    - Config changes use update_mint, not monkeypatch
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Port test_verify.py — LUD-21 verify endpoint</name>
  <files>
    tests/test_verify.py
  </files>
  <read_first>
    <file>~/lnurl-mint/tests/test_verify.py</file>
    <file>tests/test_poc_verify_race.py</file>
    <file>tests/conftest.py</file>
  </read_first>
  <behavior>
    - tests/test_verify.py contains 15 ported tests (2 migration tests are N/A — skipped)
    - Tests cover: verify URL advertisement (absent by default, advertised with comment, absent without comment), settlement status (unsettled, settled, preimage withholding, unknown hash, settled-after-spend), off-switch (disabled entirely), melt verify (no verify by default, pr+verify when enabled, settled+preimage when paid, unsettled while pending, settled after finalized, disabled when off)
    - All tests use direct endpoint calls: get_verify, get_pay_callback, get_withdraw_callback
    - verify_enabled is toggled via update_mint, not monkeypatch
    - Migration tests (test_mints_table_migrates_from_before_lud21, test_melts_table_migrates_from_before_mark_melt_settled) are NOT ported — N/A (port uses LNbits migrations from start)
  </behavior>
  <action>
    Create `tests/test_verify.py` with 15 ported tests from source test_verify.py. Skip the 2 migration tests (N/A). For each test:
    - Replace `client.get("/verify/{ph}")` → `await get_verify(TEST_MINT_ID, ph)`
    - Replace `client.get("/p/cb?...")` → `await get_pay_callback(TEST_MINT_ID, _mock_request(), amount=X, comment=Y)`
    - Replace `client.get("/w/cb?...")` → `await get_withdraw_callback(TEST_MINT_ID, _mock_request(), BackgroundTasks(), k1=[k1], ...)`
    - Replace `monkeypatch.setattr(settings, "verify_enabled", True)` → `await update_mint(TEST_MINT_ID, TEST_WALLET, verify_enabled=True)`
    - Replace `node.last_preimage.hex()` → `node.preimages[payment_hash]`
    - Replace `node.melt_preimages[ph].hex()` → use `node.preimages` or `get_standalone_payment` mock for melt preimage
    - Replace `notes.note_spent(id)` → `await get_note(id)` and check state == "spent"
    - For melt verify tests, use `inflight_node` for pending state tests
  </action>
  <verify>
    <automated>cd /home/exedev/lnurlmint && python -m pytest tests/test_verify.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 15 ported tests in test_verify.py pass. Migration tests are not ported (N/A).</done>
</task>

<task type="auto">
  <name>Task 4: Port test_comment_protection.py — LUD-25 comment protection</name>
  <files>
    tests/test_comment_protection.py
  </files>
  <read_first>
    <file>~/lnurl-mint/tests/test_comment_protection.py</file>
    <file>tests/conftest.py</file>
  </read_first>
  <behavior>
    - tests/test_comment_protection.py contains 11 ported tests from source test_comment_protection.py
    - Tests cover: commentAllowed advertisement, valid comment credits note under secret, valid comment redeems normally, missing comment fallback, malformed comment fallback, verify advertised only with valid comment, informational GET lazily settles, unsettled comment-protected mint not yet a note, comment collision with outstanding note rejected, comment collision with pending mint rejected, comment-protected note can split/rotate/merge
    - Uses mint_note_with_comment helper from conftest.py
    - All tests use direct endpoint calls
  </behavior>
  <action>
    Create `tests/test_comment_protection.py` with 11 ported tests. For each test:
    - Use `mint_note_with_comment(node, VALUE)` for comment-protected notes
    - Use `mint_note(node, VALUE)` for regular notes
    - Replace `client.get("/p/cb?...&comment=...")` → `await get_pay_callback(TEST_MINT_ID, _mock_request(), amount=X, comment=Y)`
    - Replace `client.get("/w?k1=...")` → `await get_withdraw(TEST_MINT_ID, _mock_request(), k1=k1)`
    - Replace `notes.note_amount(h)` → `await get_note(h)` and check `.amount_msat`
    - Replace `notes.pending_mint(ph)` → `await get_pending_mint_record(ph)`
    - Replace `monkeypatch.setattr(settings, "verify_enabled", True)` → `await update_mint(TEST_MINT_ID, TEST_WALLET, verify_enabled=True)`
  </action>
  <verify>
    <automated>cd /home/exedev/lnurlmint && python -m pytest tests/test_comment_protection.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 11 tests in test_comment_protection.py pass.</done>
</task>

<task type="auto">
  <name>Task 5: Port test_surface_hunter_verification.py + test_auth_data_hunter_poc.py</name>
  <files>
    tests/test_surface_hunter.py
    tests/test_auth_data_hunter.py
  </files>
  <read_first>
    <file>~/lnurl-mint/tests/test_surface_hunter_verification.py</file>
    <file>~/lnurl-mint/tests/test_auth_data_hunter_poc.py</file>
    <file>tests/test_poc_verify_race.py</file>
    <file>tests/test_poc_a1_collision_griefing.py</file>
  </read_first>
  <behavior>
    - tests/test_surface_hunter.py contains 4 ported tests (1 N/A: P2 RPC caching census)
    - tests/test_auth_data_hunter.py contains 3 ported tests
    - Surface hunter tests: P3 rotate onto pending mint rejected, P1 verify no longer hands out no-comment secret, P1b verify harmless with comment protection, P6 pathological ppm raises (adapted — may be synchronous test without threading)
    - P2 RPC amplification test is N/A — the port doesn't have cached_fetch_node_info or the same RPC patterns
    - Auth data hunter tests: F1 verify disclosure requires verify_enabled, F3 withdraw rejects pending with spec reason (use inflight_node), F4 rotate onto pending mint rejected
    - All tests use direct endpoint calls
  </behavior>
  <action>
    Create `tests/test_surface_hunter.py` with 4 tests:
    1. test_p3_rotate_onto_pending_mint_is_rejected — mint attacker note, create victim pending mint, rotate attacker note onto victim ph → ERROR, victim mint materializes
    2. test_p1_verify_no_longer_hands_out_no_comment_fallback_secret — verify_enabled=True, no comment → no verify, /verify 404, victim rotates normally
    3. test_p1b_verify_is_harmless_once_comment_protection_is_used — verify_enabled=True, comment → verify served, stolen preimage redeems nothing, victim secret redeems
    4. test_p6_pathological_ppm_raises_instead_of_hanging — call _min_sendable_msat with pathological ppm → RuntimeError (synchronous, no threading needed if it raises immediately)

    Create `tests/test_auth_data_hunter.py` with 3 tests:
    1. test_f1_verify_disclosure_requires_verify_enabled — verify_enabled=False → no verify, /verify 404, victim rotates
    2. test_f3_withdraw_rejects_pending_note_with_spec_reason — use inflight_node, melt in background, concurrent get_withdraw → "pending"
    3. test_f4_rotate_onto_pending_mint_rejected_victim_unharmed — same as P3 but standalone
  </action>
  <verify>
    <automated>cd /home/exedev/lnurlmint && python -m pytest tests/test_surface_hunter.py tests/test_auth_data_hunter.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 7 tests (4 surface hunter + 3 auth data hunter) pass.</done>
</task>

<task type="auto">
  <name>Task 6: Port test_poc_a3_mark_pending.py + test_reconcile.py</name>
  <files>
    tests/test_mark_pending.py
    tests/test_reconcile.py
  </files>
  <read_first>
    <file>~/lnurl-mint/tests/test_poc_a3_mark_pending.py</file>
    <file>~/lnurl-mint/tests/test_reconcile.py</file>
    <file>tests/conftest.py</file>
    <file>tests/test_poc_reconcile_inflight_race.py</file>
  </read_first>
  <behavior>
    - tests/test_mark_pending.py contains 3 ported tests from source test_poc_a3_mark_pending.py
    - tests/test_reconcile.py contains 4 ported tests from source test_reconcile.py (3 server-lifespan tests adapted to function calls)
    - Mark pending tests: HTTP melt rejects multiple k1s, mark_pending validates every id at any position, finalize/restore are no-ops on unknown ids
    - Reconcile tests: finalize pending once confirmed paid, restore pending once confirmed not paid, leave unconfirmable pending without retrying, error logging for unconfirmed notes
    - Server-lifespan tests (boot reconcile, periodic monitor, monitor survives failure) are adapted to call reconcile_pending_melts directly
    - All tests use direct calls to crud functions (mark_pending, finalize_melt, restore) and services (reconcile_pending_melts)
  </behavior>
  <action>
    Create `tests/test_mark_pending.py` with 3 tests:
    1. test_a3_http_melt_rejects_multiple_k1s_before_any_reservation — get_withdraw_callback with k1=[k1a, k1b], pr=pr → ERROR, both notes still spendable
    2. test_a3_mark_pending_validates_every_id_at_any_position — call mark_pending directly with [real_id, GARBAGE_ID] → ValueError; [GARBAGE_ID, real_id] → ValueError; [real_id, spent_id] → ValueError; [real_id, pending_id] → PendingNoteError
    3. test_a3_finalize_and_restore_on_never_reserved_ids_are_noops — call finalize_melt([GARBAGE_ID]) → no effect; call restore([GARBAGE_ID]) → no effect; control: finalize_melt on outstanding note burns it

    Create `tests/test_reconcile.py` with 4 tests:
    1. test_reconcile_finalizes_a_pending_note_once_confirmed_paid — use _leave_a_note_pending, then set node.payment_actually_completed=True, call await reconcile_pending_melts(), check note is burned
    2. test_reconcile_restores_a_pending_note_once_confirmed_not_paid — use _leave_a_note_pending, then set node to return PaymentFailedStatus, call await reconcile_pending_melts(), check note is restored
    3. test_reconcile_leaves_still_unconfirmable_notes_pending_without_retrying — use _leave_a_note_pending, keep is_payment_complete_raises=True, call await reconcile_pending_melts(), check node.check_transaction_status_calls == 1, note still pending
    4. test_reconcile_writes_still_unconfirmed_notes_to_error_log — use _leave_a_note_pending, call await reconcile_pending_melts(), capture loguru output, check "still unconfirmed" in logs, check k1 not in logs

    For reconcile tests, import reconcile_pending_melts from lnurlmint.services. Use loguru sink capture for error logging test (same pattern as test_offline_verification.py's test_signing_failure_is_still_logged).
  </action>
  <verify>
    <automated>cd /home/exedev/lnurlmint && python -m pytest tests/test_mark_pending.py tests/test_reconcile.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 7 tests (3 mark_pending + 4 reconcile) pass.</done>
</task>

<task type="auto">
  <name>Task 7: Run full test suite as regression gate</name>
  <files>
  </files>
  <read_first>
  </read_first>
  <behavior>
    - The full test suite (all existing tests + all new tests from Plans 07-01 and 07-02) passes without regressions
    - The suite includes: test_bearer_threat_suite.py (8), test_lnurlcash.py (~55), test_verify.py (15), test_comment_protection.py (11), test_surface_hunter.py (4), test_auth_data_hunter.py (3), test_mark_pending.py (3), test_reconcile.py (4), plus all existing tests from Phases 2-6
    - No test is skipped or xfail without justification
  </behavior>
  <action>
    Run the full test suite: `cd /home/exedev/lnurlmint && python -m pytest tests/ -v --tb=short 2>&1 | tail -50`

    Verify:
    1. All previously passing tests still pass
    2. All new tests from Plan 07-01 (8 threat suite tests) pass
    3. All new tests from Plan 07-02 (~95 tests across 7 files) pass
    4. Total test count is approximately 64 (existing) + 8 (07-01) + 95 (07-02) = ~167 tests
    5. No regressions in any existing test

    If any test fails, investigate and fix before marking this task done. Common issues:
    - Missing conftest.py helpers → add them
    - Endpoint function signature mismatch → check views_lnurl.py for exact parameters
    - FakeNode missing attributes → add them to conftest.py
    - Async/sync mismatch → ensure all async tests have @pytest.mark.anyio
  </action>
  <verify>
    <automated>cd /home/exedev/lnurlmint && python -m pytest tests/ -v --tb=short 2>&1 | tail -50</automated>
  </verify>
  <done>Full test suite passes with no regressions. All ~167 tests green.</done>
  <acceptance_criteria>
    - `python -m pytest tests/ -v` shows all tests passed
    - No existing test regressed
    - All new test files are in the output
    - Total test count is approximately 167 (64 existing + 8 threat suite + 95 remaining)
  </acceptance_criteria>
</task>

</tasks>

<skip_list>
## Already-Ported Tests (Phases 2-6) — Skip

| Source File | Phase | Ported As |
|-------------|-------|-----------|
| test_melt_restore_double_payout_poc.py | Phase 2 | test_melt_restore_double_payout_poc.py |
| test_poc_duplicate_melt.py | Phase 2 | test_poc_duplicate_melt.py |
| test_poc_a2_settle_race.py | Phase 2 | test_poc_a2_settle_race.py |
| test_poc_f2_pending_info_leak.py | Phase 2 | test_poc_f2_pending_info_leak.py |
| test_poc_reconcile_inflight_race.py | Phase 2 | test_poc_reconcile_inflight_race.py |
| test_poc_fee_conservation.py | Phase 3 | test_poc_fee_conservation.py |
| test_poc_fee_loop.py | Phase 3 | test_poc_fee_loop.py |
| test_poc_a1_collision_griefing.py | Phase 3 | test_poc_a1_collision_griefing.py |
| test_poc_verify_race.py | Phase 4 | test_poc_verify_race.py |
| test_offline_verification.py | Phase 5 | test_offline_verification.py |
| test_onion.py | Phase 6 | test_onion.py |

## N/A Tests — Not Ported (LNbits Extension Model)

| Source File | Tests | Justification |
|-------------|-------|---------------|
| test_config.py | 1 | Standalone config model (BASE_URL required) replaced by per-mint DB rows + LNbits settings |
| test_errors.py | 3 | errors.py module (file-based error logging) replaced by loguru + FastAPI exception handlers |
| test_frontend.py | 24 | Server-rendered HTML (Jinja2) replaced by Vue SPA; API endpoints tested by test_public_api.py + test_management_api.py |
| test_mint_log.py | 6 | mint_log.py module (file-based activity logging) replaced by loguru + activity API (test_management_api.py) |
| test_node.py | 19 | node.py (direct lnd/cln REST client) replaced by LNbits Wallet abstraction; tristate tested via FakeNode fixtures |
| test_server.py | 8 | server.py lifespan (CORS, access logger, health monitor) replaced by LNbits lifecycle |
| test_poc_rpc_census.py | 8 | Per-endpoint RPC census (lnd/cln direct calls) — RPC patterns differ in LNbits model |
</skip_list>
