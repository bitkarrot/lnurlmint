---
phase: 07-full-test-suite-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_bearer_threat_suite.py
autonomous: true
requirements:
  - TEST-09
must_haves:
  truths:
    - The full bearer-asset threat suite (test_bearer_threat_suite_poc.py) is ported and passes against LNbits fixtures.
    - T2 routing-node race (no-comment fallback) documents the vulnerable behavior — preimage alone redeems.
    - T2b comment-protected note defeats the routing-node race — preimage never became the note's k1.
    - T3 informational poll leaks the live note — documents current behavior (INVERTS WHEN option D).
    - T4 callback log replay fails — control test, must hold under every option.
    - T5 note at rest is cash — control test, the bearer axiom.
    - T6 operator can link rotate to later spend — privacy row, only blinded signatures win.
    - T10 merge URL budget — pure URL arithmetic, plaintext fits, encrypted does not.
    - T9 comment silently ignored — documents option B landing (malformed comment falls back cleanly, no verify advertised).
  artifacts:
    - path: tests/test_bearer_threat_suite.py
      provides: Full bearer-asset threat suite ported from source test_bearer_threat_suite_poc.py
      contains: "test_t2_routing_node_race"
    - path: tests/conftest.py
      provides: note_value async helper and any additional fixtures needed by the threat suite
      contains: "async def note_value"
  key_links:
    - from: tests/test_bearer_threat_suite.py
      to: lnurlmint/views_lnurl.py
      via: "get_withdraw, get_withdraw_callback, get_pay_callback direct calls"
      pattern: "get_withdraw|get_pay_callback"
    - from: tests/test_bearer_threat_suite.py
      to: lnurlmint/crud.py
      via: "get_note for checking note amounts after rotate/melt"
      pattern: "get_note"
    - from: tests/test_bearer_threat_suite.py
      to: tests/conftest.py
      via: "mint_note, mint_note_with_comment, fresh_secret, node, db_setup fixtures"
      pattern: "mint_note|fresh_secret"
---

## Phase Goal

**As a** security reviewer, **I want to** run the full bearer-asset threat suite against LNbits fixtures, **so that** the integration-level security gate verifies no funds-loss regression across all individual PoC guarantees.

## Acceptance Criteria

- [ ] `tests/test_bearer_threat_suite.py` is created with 8 ported test functions from `test_bearer_threat_suite_poc.py`.
- [ ] T2 (routing-node race, no-comment fallback) passes — attacker rotates with preimage, succeeds in no-comment fallback.
- [ ] T2b (comment-protected defeats race) passes — attacker's rotate with preimage fails; WALLET-held secret redeems.
- [ ] T3 (informational poll leaks) passes — GET /w with live k1, attacker replays from log, succeeds today.
- [ ] T4 (callback log replay fails — control) passes — spent k1 cannot be replayed.
- [ ] T5 (note at rest is cash — control) passes — finder of URL spends it.
- [ ] T6 (operator correlation) passes — mint storage key matches h, later spend links to issuance.
- [ ] T10 (merge URL budget) passes — 25 plaintext k1s fit in 2000 chars, 25 encrypted blobs don't.
- [ ] T9 (comment silently ignored) passes — malformed comment falls back to preimage-keyed note, no verify advertised.
- [ ] All 8 tests pass with `@pytest.mark.anyio` using direct endpoint calls (no TestClient).
- [ ] `tests/conftest.py` has a `note_value` async helper if needed.

## Artifacts This Phase Produces

This plan produces the bearer-asset threat suite — the integration-level security gate that combines all individual PoC guarantees into one executable scorecard. Each test corresponds to a row in the LUD-25 design debate scorecard, documenting which attacks succeed today and which are mitigated by comment protection (option B). This is the final security gate before the full regression suite.

<threat_model>
ASVS Level: 1 | Blocking Threshold: high

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Routing-node preimage race (T2) | high | T2 documents the vulnerable no-comment fallback; T2b proves comment protection defeats it — the note is keyed by WALLET-held secret, not the preimage |
| Informational poll log leak (T3) | high | T3 documents the current behavior (k1 in URL logs); INVERTS WHEN option D (hash-keyed GET) lands — pinned so the fix is measurable |
| Bearer axiom regression (T5) | medium | T5 is a control test — the bearer property (note URL = money) must never change; pinned so any future option claiming to fix at-rest exposure answers this test first |
</threat_model>

<objective>
Port the full bearer-asset threat suite (test_bearer_threat_suite_poc.py) to LNbits fixtures, proving the integration-level security gate passes with all individual PoC guarantees combined.

Purpose: This is the executable scorecard for the LUD-25 design debate — one test per attack scenario, measuring which options mitigate which attacks. Passing this suite is the final security gate.
Output: A passing test_bearer_threat_suite.py with 8 tests covering T2, T2b, T3, T4, T5, T6, T9, T10.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-full-test-suite-port/07-CONTEXT.md
@.planning/phases/07-full-test-suite-port/07-RESEARCH.md
@tests/conftest.py
@tests/test_poc_verify_race.py
@tests/test_offline_verification.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add note_value helper to conftest.py</name>
  <files>
    tests/conftest.py
  </files>
  <read_first>
    <file>tests/conftest.py</file>
    <file>~/lnurl-mint/tests/test_lnurlcash.py</file>
  </read_first>
  <behavior>
    - tests/conftest.py contains an async helper `note_value(k1: str) -> int | None` that calls `get_withdraw(TEST_MINT_ID, _mock_request(), k1=k1)` and returns `maxWithdrawable` if the response has no error, or `None` if the response is an error
    - The helper uses a mock Request object (MagicMock with base_url set) consistent with the _mock_request pattern in other test files
    - The helper is importable from `lnurlmint.tests.conftest`
  </behavior>
  <action>
    Add an async `note_value(k1)` function to `tests/conftest.py` that:
    1. Creates a mock request (MagicMock with `base_url = "http://test/"`)
    2. Calls `await get_withdraw(TEST_MINT_ID, req, k1=k1)`
    3. If the result dict has `"status" == "ERROR"`, returns `None`
    4. Otherwise returns `result["maxWithdrawable"]`
    Import `get_withdraw` from `lnurlmint.views_lnurl` and `MagicMock` from `unittest.mock` at the top of the file if not already imported.
  </action>
  <verify>
    <automated>python -c "from lnurlmint.tests.conftest import note_value; print(callable(note_value))"</automated>
  </verify>
  <done>The note_value helper is defined and importable.</done>
</task>

<task type="auto">
  <name>Task 2: Port test_bearer_threat_suite_poc.py — T2, T2b, T3, T4, T5, T6, T9, T10</name>
  <files>
    tests/test_bearer_threat_suite.py
  </files>
  <read_first>
    <file>~/lnurl-mint/tests/test_bearer_threat_suite_poc.py</file>
    <file>tests/conftest.py</file>
    <file>tests/test_poc_verify_race.py</file>
    <file>tests/test_offline_verification.py</file>
    <file>.planning/phases/07-full-test-suite-port/07-RESEARCH.md</file>
  </read_first>
  <behavior>
    - tests/test_bearer_threat_suite.py contains exactly 8 async test functions with @pytest.mark.anyio
    - test_t2_routing_node_race_p_alone_is_sufficient: mints a no-comment note (mint_note), attacker rotates with the preimage k1 → status OK, attacker gets the value; victim arrives later → ERROR "Invalid or already spent k1"
    - test_t2b_comment_protected_note_defeats_the_routing_node_race: mints a comment-protected note (mint_note_with_comment), attacker rotates with the preimage → ERROR; victim rotates with their secret → OK
    - test_t3_informational_poll_leaks_the_live_note: mints a note, victim checks value via get_withdraw (informational poll), attacker replays k1 from "log" → rotate OK (documents current behavior)
    - test_t4_callback_log_replay_fails_control: mints a note, rotates it (burns k1), attacker replays the burned k1 → ERROR (control: burned k1 can't be replayed)
    - test_t5_note_at_rest_is_cash_control: mints a note, finder rotates with k1 → OK (control: bearer axiom, note URL = money)
    - test_t6_operator_can_link_rotate_to_later_spend: mints a note, rotates to h, checks that the mint's storage key for the new note is verbatim h (operator correlation), then spends new_k1 and checks it matches h
    - test_t10_merge_url_budget_plaintext_fits_encrypted_does_not: pure URL arithmetic, no endpoint calls — 25 plaintext k1s in GET URL <= 2000 chars, 25 encrypted blobs > 2000 chars
    - test_t9_comment_is_silently_ignored_today: sets verify_enabled=True via update_mint, calls get_pay_callback with a non-hex32 comment → pr returned, no "verify" in response; settles, rotates with preimage → OK (malformed comment falls back cleanly)
    - All tests use direct endpoint calls: get_withdraw, get_withdraw_callback, get_pay_callback
    - All tests use node, db_setup, fresh_secret, mint_note, mint_note_with_comment from conftest.py
    - T9 uses update_mint to set verify_enabled=True instead of monkeypatching settings
    - T6 uses get_note to check the mint's storage key (note_id == h)
    - T10 is a pure arithmetic test with no fixtures needed (no @pytest.mark.anyio needed)
  </behavior>
  <action>
    Create `tests/test_bearer_threat_suite.py` with the following 8 tests, adapted from the source:

    1. **test_t2_routing_node_race_p_alone_is_sufficient** — `@pytest.mark.anyio`. Uses `mint_note(node, 50_000)` to get k1. Attacker: `fresh_secret()` for h, `await get_withdraw_callback(TEST_MINT_ID, _mock_request(), BackgroundTasks(), k1=[k1], h=attacker_h)` → assert status OK. Check `await get_note(attacker_h)` has amount 50_000. Victim: `fresh_secret()`, `await get_withdraw_callback(..., k1=[k1], h=victim_h)` → assert ERROR.

    2. **test_t2b_comment_protected_note_defeats_the_routing_node_race** — `@pytest.mark.anyio`. Uses `mint_note_with_comment(node, 50_000)` to get `(victim_secret, comment_hash, note_id, payment_hash, mint)`. Get preimage from `node.preimages[payment_hash]`. Attacker: `await get_withdraw_callback(..., k1=[preimage], h=attacker_h)` → assert ERROR. Check `await get_note(attacker_h)` is None. Victim: `await get_withdraw_callback(..., k1=[victim_secret], h=victim_h)` → assert OK. Check `await get_note(victim_h)` has amount 50_000.

    3. **test_t3_informational_poll_leaks_the_live_note** — `@pytest.mark.anyio`. Uses `mint_note(node, 50_000)`. Victim: `await get_withdraw(TEST_MINT_ID, _mock_request(), k1=k1)` → assert tag withdrawRequest, maxWithdrawable 50_000. Attacker: `fresh_secret()`, `await get_withdraw_callback(..., k1=[k1], h=attacker_h)` → assert OK (documents current behavior). Check `await get_note(attacker_h)` has amount 50_000.

    4. **test_t4_callback_log_replay_fails_control** — `@pytest.mark.anyio`. Uses `mint_note(node, 50_000)`. Rotate: `fresh_secret()` for (new_k1, h), `await get_withdraw_callback(..., k1=[k1], h=h)` → assert OK. Check `await get_note(sha256(new_k1))` has amount 50_000. Attacker: `fresh_secret()`, `await get_withdraw_callback(..., k1=[k1], h=attacker_h)` → assert ERROR. Check `await get_note(h)` still has amount 50_000 (rotated note untouched).

    5. **test_t5_note_at_rest_is_cash_control** — `@pytest.mark.anyio`. Uses `mint_note(node, 50_000)`. Finder: `fresh_secret()`, `await get_withdraw_callback(..., k1=[k1], h=finder_h)` → assert OK. Check `await get_note(finder_h)` has amount 50_000.

    6. **test_t6_operator_can_link_rotate_to_later_spend** — `@pytest.mark.anyio`. Uses `mint_note(node, 50_000)`. Rotate: `fresh_secret()` for (new_k1, h), `await get_withdraw_callback(..., k1=[k1], h=h)` → assert OK. Check `await get_note(h)` has amount 50_000 (mint's storage key is verbatim h). Assert `sha256(bytes.fromhex(new_k1)).hexdigest() == h`. Spend: `fresh_secret()` for h2, `await get_withdraw_callback(..., k1=[new_k1], h=h2)` → assert OK.

    7. **test_t10_merge_url_budget_plaintext_fits_encrypted_does_not** — No fixtures, no @pytest.mark.anyio. Pure URL arithmetic: `base = "http://testserver/w/cb?"`, `h_param = "&h=" + "0"*64`. Plaintext: `base + "&".join(f"k1={'a'*64}" for _ in range(25)) + h_param`, assert `len(plaintext) <= 2000`. Encrypted: `blob = "A"*124`, `base + "&".join(f"p={blob}" for _ in range(25)) + h_param`, assert `len(encrypted) > 2000`.

    8. **test_t9_comment_is_silently_ignored_today** — `@pytest.mark.anyio`. First `await update_mint(TEST_MINT_ID, TEST_WALLET, verify_enabled=True)`. Call `await get_pay_callback(TEST_MINT_ID, _mock_request(), amount=50_000, comment="this-will-be-ignored")` → assert pr in response, assert "verify" not in response. Settle: `node.settled.add(payment_hash)` where payment_hash = sha256(preimage). Get k1 = preimage hex from `node.preimages[payment_hash]`. Rotate: `fresh_secret()`, `await get_withdraw_callback(..., k1=[k1], h=h)` → assert OK.

    Import needed: `pytest`, `BackgroundTasks` from fastapi, `sha256` from hashlib, `MagicMock` from unittest.mock, `get_withdraw`, `get_withdraw_callback`, `get_pay_callback` from `lnurlmint.views_lnurl`, `get_note`, `update_mint` from `lnurlmint.crud`, `TEST_MINT_ID`, `TEST_WALLET`, `fresh_secret`, `mint_note`, `mint_note_with_comment`, `node`, `db_setup` from `lnurlmint.tests.conftest`.

    Add a `_mock_request()` helper at the top of the file (same as other test files): returns MagicMock with base_url = "http://test/".
  </action>
  <verify>
    <automated>cd /home/exedev/lnurlmint && python -m pytest tests/test_bearer_threat_suite.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 8 tests in test_bearer_threat_suite.py pass against LNbits fixtures.</done>
  <acceptance_criteria>
    - `python -m pytest tests/test_bearer_threat_suite.py -v` shows 8 passed
    - No test uses TestClient or client fixture
    - All async tests use @pytest.mark.anyio
    - T10 is a pure arithmetic test with no async marker
    - T9 uses update_mint for verify_enabled, not monkeypatch
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run full test suite to verify no regressions</name>
  <files>
  </files>
  <read_first>
  </read_first>
  <behavior>
    - The full test suite (all existing tests + new threat suite) passes without regressions
    - Total test count increases by 8 (the new threat suite tests)
  </behavior>
  <action>
    Run the full test suite: `cd /home/exedev/lnurlmint && python -m pytest tests/ -v --tb=short 2>&1 | tail -30`

    Verify that all previously passing tests still pass and the 8 new tests pass. If any existing test fails, investigate and fix the regression before marking this task done.
  </action>
  <verify>
    <automated>cd /home/exedev/lnurlmint && python -m pytest tests/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>All tests pass (existing + 8 new threat suite tests), no regressions.</done>
  <acceptance_criteria>
    - `python -m pytest tests/ -v` shows all tests passed
    - No existing test regressed
    - 8 new tests from test_bearer_threat_suite.py are in the output
  </acceptance_criteria>
</task>

</tasks>
